var conn = require('connections');
var Crud = {};

Crud = (function ( ) {
  return {

    //findall
    findall : function ( table ) {
      return
    },

    //findid
    findid : function ( id ) {
      return
    },

    //delete
    delete : function ( id ) {
      return
    },

    //update
    update : function ( id ) {
      return
    },

    //create
    create : function ( id ) {
      return
    },
  }
}());

module.exports = Crud;
